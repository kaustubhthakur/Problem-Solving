#include <bits/stdc++.h>
using namespace std;
int f[10000001];
const int p = 1e8;
int c (int n,int k)
{
    if(k>n)
    {
        return 0;
    }
    else 
    {
        return 
    }
}
int main()
{
    f[0]=f[1]=1;
    for(int i=1;i<=1000000;++i)
    {
      f[i] = (f[i]*1LL*i)%p; 
    }
    int q;
    int n,k;
    while(q--)
    {
        cin>>n>>k;
        cout<<
    }

    return 0;
}